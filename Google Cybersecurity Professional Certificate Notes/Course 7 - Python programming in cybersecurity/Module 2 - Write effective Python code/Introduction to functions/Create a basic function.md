Defining a Function

- Use the `def` keyword followed by the function name and parentheses, ending with a colon.
- Indent the code block that specifies what the function will do.

Calling a Function

- Defining a function only tells Python it exists; to execute its code, you must call it.
- To call a function, simply write its name followed by parentheses.